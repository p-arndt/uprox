services:
  db:
    image: postgres
    restart: unless-stopped 
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: 
      POSTGRES_PASSWORD: 
      POSTGRES_DB:
    volumes:
      - pgdata:/var/lib/postgresql
volumes:
  pgdata:
